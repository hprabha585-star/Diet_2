FastCoach — update 2 (plans, gating, alerts, chat, contact, BMI, mobile)
=======================================================================

NEW BACKEND
  models/Plan.js            coach-editable plans/pricing (replaces hard-coded tiers)
  models/Alert.js           coach announcements (one client or whole cohort)
  models/Message.js         coach <-> client chat
  models/Settings.js        "Contact us" details shown to clients

CHANGED BACKEND
  models/User.js            age, gender, healthyWeightRange(); tier is now a plan key
  models/Payment.js         tier is a plan key + planName snapshot
  routes/client.js          plans, payment status, gating (requireActive), checklist
                            sync with the coach's plan, alerts, chat, contact, profile
  routes/admin.js           plans CRUD, alerts, chat threads, referral overview,
                            wallet adjust, contact settings, plan duration on approval
  utils/helpers.js          unique checklist keys (duplicate meals/habits no longer collide)
  server.js                 public GET /api/plans for the landing page

CHANGED FRONTEND
  index.html                show/hide password, pricing driven by the coach's plans
  client/dashboard.html     locked state, split fasting/eating panels, alerts, chat,
                            contact, plan picker, BMI with age + gender
  admin/dashboard.html      referral overview, plans, alerts, messages, contact details
  js/client.js              all of the above
  js/admin.js               all of the above
  css/dashboard.css         window split, alerts, chat, plans, contact, mobile pass
  css/landing.css           password toggle + mobile pass

WHAT TO DO AFTER DEPLOYING
  1. Admin console -> Contact details: fill in your UPI ID, phone, WhatsApp, email.
     The UPI ID appears on the client's payment screen.
  2. Admin console -> Plans & pricing: the two original tiers are created
     automatically the first time the page loads. Edit prices, add your own plans.
  3. Admin console -> 55-day protocol -> Load defaults (if you haven't already).
  4. Hard-refresh the client app so the new CSS/JS load.
